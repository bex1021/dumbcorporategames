# Blocked — game source

The Vite + React + TypeScript app behind [dumbcorporategames.com](https://dumbcorporategames.com). See the [root README](../README.md) for what the game is and how the repo is laid out.

```bash
npm install
npm run dev
```

- `TYPECHECKING.md` — how type checks are run in this project
- `MIXAMO_WORKFLOW.md` — character/animation pipeline from Mixamo FBX to compressed GLB
